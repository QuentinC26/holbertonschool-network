#!/usr/bin/env bash
# i change the port in ect/hosts of localhost and facebook
cp /etc/hosts /etc/hosts.old
sed -i 's/127.0.0.1/127.0.0.2/' /etc/hosts
sed -i sudo sed -i '/facebook\.com/d' /etc/hosts
echo 'facebook.com  8.8.8.8' | sudo tee -a /etc/hosts > /dev/null
